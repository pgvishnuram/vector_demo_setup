# Vector ElasticSearch Configuration

# Download Vector

```
# For all other os platform
curl --proto '=https' --tlsv1.2 -sSf https://sh.vector.dev | bash -s -- -y
```

```
# For MAC users

brew tap vectordotdev/brew && brew install vector
```
